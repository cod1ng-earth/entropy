import styled from "styled-components";

const Link = styled.a`
    color: turquoise;
    text-decoration: underline;
    cursor: pointer;
    &:hover {
        color: aqua;
    }
`

export default Link;